# Basic Inventory Example

An example of how to do pickups and drops using the new Inventory API in Core.

Pickups are not optimized at all, so lots of them will use up the networked object count quickly.
